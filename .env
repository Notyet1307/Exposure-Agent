# Local development uses the backend's directly exposed port. Production uses the same-origin Nginx proxy.
FRONTEND_HOST=http://localhost:5173

ENVIRONMENT=local

PROJECT_NAME=Exposure-Agent
STACK_NAME=exposure-agent

BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173"
SECRET_KEY=not-for-production-issue4-secret
FIRST_SUPERUSER=admin@example.com
FIRST_SUPERUSER_PASSWORD=not-for-production-issue4-admin

# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=not-for-production-issue4-postgres

# Configure these with your own Docker registry images
DOCKER_IMAGE_BACKEND=backend
DOCKER_IMAGE_FRONTEND=frontend
