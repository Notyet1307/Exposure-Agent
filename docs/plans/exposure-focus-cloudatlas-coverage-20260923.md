# 云图资产、风险与依据覆盖矩阵（EXP-FOCUS-00 初版）

日期：2026-09-23；读取基线 `71f65e9053e01bf4cb97cafe5ee5e538c9c34db2`。**批准Spec的完整性附件，非完整同步验收结论**；配套[聚焦Spec](../specs/exposure-focus-release-1.md)。01A正式任务由[当前入口](../work/current.md)定位，[候选正文](exp-focus-01a-issue-candidate.md)仅保留接管时快照。文档发布不改变本矩阵的证据等级；本次接管/发布均无真实云图、业务数据库或模型调用。

## 已找到的材料与证据强度

| 编号 | 已有材料 | 能证明什么 / 不能证明什么 |
|---|---|---|
| E1 | [cloudatlas_read.proto](../../octobus/cloudatlas-read/proto/cloudatlas_read.proto) 5—26行 | 产品只有 `cloudatlas.read.v1.CloudAtlasReadService/ListIPAssets`；请求status/page/size；IPAsset仅id/ip/status；返回items/page/size/total。没有资产详情、风险或附件方法 |
| E2 | [cloudatlas-read.js](../../octobus/cloudatlas-read/bin/cloudatlas-read.js) 19—46、52—85、99—127行 | 使用现有CLI `cloudAtlas asset ip list`；从config的baseUrl/spaceId及secret token形成隔离CLI配置；CLI返回`items[].id/ip/status`，id整数转字符串，`current`映射`page`，多余属性未传入产品协议。允许status valid/await/ignored/invalid、size≤200；这些是代码限制，不证明目标云图版本全部状态语义 |
| E3 | [fixture_upstream.py](../../tests/cloudatlas_fixture/fixture_upstream.py) 123—185行、[verify.py](../../tests/cloudatlas_fixture/verify.py)、[fixture脚本](../../scripts/test-cloudatlas-fixture.sh)、[OctoBus说明](../../octobus/README.md) | 合成GET `/openapi/v1/asset/ip`，TOKEN header，响应`data.current/items/size/total`；fixture有401/403/503/坏结构/断连分支。**HTTP路径和data包裹层来自合成fixture，不升级为本轮目标版本正式接口文档**；不是风险API资料 |
| E4 | [cloudatlas_ledger.py](../../backend/app/domain/cloudatlas_ledger.py) 88—146、169—214、263—330、360—421行；[API测试](../../backend/tests/api/routes/test_cloudatlas_ledger.py) | 已发布Run的SourceSnapshot/Observation本地读模型；asset_id/raw_ip/canonical_ip/source_status，本地tags/followed分层；count/total_records/unique_ips分开；risk_state固定NOT_CONNECTED。source_created_at来自本地snapshot.created_at，非上游更新时间。测试存在不等于本轮运行通过 |
| E5 | [真实旧回执 #240](https://github.com/Notyet1307/Exposure-Agent/issues/240) 的恢复完成/部署段；[PR #241](https://github.com/Notyet1307/Exposure-Agent/pull/241) | 2026-09-15历史回执：指定来源valid共66页13,182条，IPv6 30条；Runner落地与逐页本地读，网关停止后固定快照仍可读。只限旧IP合同；不是风险/详情/全状态验证，不证明ID长期稳定、分页快照、现时权限或部署。原材料在既有仓库外私有存储；本机未发现所引用历史部署证据目录，未读取私有响应/凭据 |
| E6 | [canary runbook](../runbooks/cloudatlas-canary.md)、[ADR-0019](../adr/0019-cloudatlas-native-ledger-and-scoped-profile.md) | 已有隔离、脱敏、单方法授权及真实核验方法；旧流程要求客户输入和Run，不能直接代替独立同步新合同。未执行runbook |

资料检索范围：仓库docs/runbooks、Spec/ADR、OctoBus包、相关backend代码/测试、tests/cloudatlas_fixture、scripts及实际#240/相邻交付PR。未在上述材料找到目标版本的完整资产详情/风险/附件正式合同或对应脱敏响应；这不等于供应商不存在接口。未搜索凭据值或扫描用户其他私有目录。

## 读表规则

- “结果”只用：**已验证可同步、待核实、接口缺失、权限缺失、明确不适用**。本表不因产品包尚未实现就断言上游“接口缺失”；缺少资料统一待核实，待01A获得事实后再分类。
- “已验证可同步”在本表只指E5的**历史指定来源/valid/IP合同**，不是本轮新需求全量验证。未知接口/字段写“未知”，不补猜测的endpoint或JSON路径。
- 标识路径分层：E3的上游fixture是`data.items[]`；E2的CLI JSON是`items[]`；E1的产品RPC是`items[]`；三者不可混称真实上游完整合同。
- “候选保存/页面”是待批准设计意图，不是当前实现。新稳定来源身份建议包含来源空间和对象身份，但实际键字段/长期稳定性仍未知；旧`source_record_key`只定位快照内行，不能用作跨批次实体键。

## 资产覆盖

| ID / 约定信息 | 实际来源及精确字段路径（未知明确写出） | 稳定键/可得性与结果 | 当前本地保存 → 候选保存/页面 | 验收方式 / 缺口 |
|---|---|---|---|---|
| A01 IP来源ID | E1/E2 `items[].id`；E3 `data.items[].id`（合成） | 历史可读、整数转字符串；**已验证可同步**仅旧合同；长期稳定/ID重用未知 | Observation.cloudatlas_asset_id → 新来源对象身份；外部资产列表/详情 | 同范围多批次/删除重建核实；G2。不用一次唯一性或同IP代替稳定ID |
| A02 IP标识 | E1/E2 `items[].ip`；E3 `data.items[].ip`（合成） | IPv4/IPv6旧回执可读；**已验证可同步**仅旧合同；IP不是跨来源稳定键 | Observation.raw_ip/canonical_ip → 保留原值及确定性规范值；搜索/详情 | mapped IPv6/重复IP/跨空间不误合并；G1/G2确认目标范围 |
| A03 来源空间/版本 | E2 `ctx.config.spaceId/baseUrl`；实际响应空间字段、产品版本字段**未知** | 配置空间有来源指纹；上游返回/授权语义未证实，**待核实** | SourceInstance及Run固定指纹 → 来源空间/合同身份；详情来源区 | 指纹不等于客户归属或共同网络空间；G1/G3 |
| A04 资产类型及非IP标识 | IP类型由当前方法限定；域名/URL/其他类型接口与字段**未知** | 类型全集及稳定键**未知**，**待核实** | 当前无此完整来源合同 → 按实际支持类型结构化保存；类型筛选/详情 | 正式能力目录/导出及代表页；G1。不得把IP-only当全部 |
| A05 归属/业务/负责人等源属性 | 云图实际字段/接口**未知**；不能用客户台账责任字段替代 | **待核实** | 无已验证云图来源字段 → 保存源声明及时间，与客户只读上下文/本地确认分栏 | 页面与脱敏响应对应；G1；不猜业务归属 |
| A06 端口与服务 | 接口/字段/服务稳定键及资产关系**未知** | **待核实** | 当前IP协议无 → 来源服务详情/资产关联 | 列表与详情覆盖对照、多个服务及关系；G1/G2 |
| A07 组件与指纹 | 接口/字段**未知** | **待核实** | 当前无 → 已约定结构化详情 | 正式字段及空值/多值/版本含义；G1 |
| A08 证书 | 接口/字段及证书身份**未知** | **待核实** | 当前无 → 约定证书属性/关联与依据 | 有效期/来源时间按真实字段；敏感材料G4 |
| A09 网站/URL详情 | 接口/字段**未知** | **待核实** | 当前无；客户Web URL不等于云图详情 → 结构化网站详情 | 代表页与响应/导出覆盖；G1 |
| A10 云图标签及其他约定属性 | 源字段**未知**；现有tags/followed为Exposure本地修订 | **待核实**，不得冒称已有上游标签 | 本地CloudAtlasLedgerRevision → 源字段/本地管理分离展示 | 两者不互相覆盖；按实际页面补全业务有价值属性；G1 |
| A11 上游状态原值 | E1/E2 `items[].status`；旧runner valid范围 | 原值**已验证可同步**仅旧合同；具体业务含义另见A12 | Observation.cloudatlas_status → 保留源原值；资产状态 | 核对过滤范围；不能由status推风险/是否受监控 |
| A12 状态定义及全部过滤范围 | adapter枚举valid/await/ignored/invalid；实际定义/可见权限/总数含义**未知** | **待核实** | 当前status_filter固定valid → 按批准合同显示范围/原状态说明 | 供应商字段文档及权限范围；G1/G3 |
| A13 发现/更新时间/观测时间 | 上游接口及路径**未知**；本地`source_created_at`来自snapshot.created_at | **待核实** | 已有本地快照时间 → 来源时间与抓取/成功时间分开；新鲜度区 | 不将本地时间伪称上游时间；时区/精度/空值G2 |
| A14 列表分页与总数 | E1 RPC page/size/total；E2 CLI current/size/total；E3 data.current/size/total | **已验证可同步**仅历史valid页码遍历；快照一致性/排序未证实 | 旧分页Artifact+快照计数 → 每域成功版本/范围与任务状态 | 多页同过滤口径对账；页中变化/空页/重复页G2；后端本地count不是上游总数 |
| A15 独立详情与列表差异 | 详情接口/字段**未知**；adapter只保留id/ip/status | **待核实** | 当前本地“详情”仍是摘要来源行 → 按合同本地详情 | 不用来源行detail路由冒充上游完整详情；G1 |

## 风险、依据及关联历史覆盖

| ID / 约定信息 | 实际接口/字段路径 | 稳定键/可得性与结果 | 当前保存 → 候选保存/页面 | 验收方式 / 缺口 |
|---|---|---|---|---|
| R01 风险列表、稳定ID | **未知**；当前RPC无风险方法，risk_state=NOT_CONNECTED | 稳定ID/去重/重用语义未知；**待核实** | 无源风险读模型 → 源风险身份、来源/版本；完整列表/分页 | 实际多页合同/总数、重复/跨空间；G1/G2 |
| R02 名称、类别、源等级 | **未知** | **待核实** | 无 → 原始值和文档语义；列表筛选/详情 | 不从资产status、Finding分类或本地优先级推导；G1 |
| R03 源状态及时间 | **未知** | **待核实** | 无 → 源状态、首次/最近时间（如实际可得）独立保存 | 状态转换、时间语义、过滤/删除含义；G1/G2；不混本地处置 |
| R04 受影响资产/服务关系 | **未知** | 风险/资产/服务关联键未知；**待核实** | 无 → 显式来源关联，暂缺保留待关联；双向跳转 | 风险先到、资产缺失、相同IP不同空间、不机械合并；G1/G2 |
| R05 描述、影响说明 | **未知** | **待核实** | 无 → 约定结构化内容/安全正文；风险详情 | 完整阅读/空值/长文，不只标题和外链；G1/G4 |
| R06 检测依据、修复建议 | **未知** | **待核实** | 无 → 来源原判断及建议，不以AI补造；风险详情/依据 | 代表风险页逐项对照；G1/G4 |
| R07 风险变更/检测历史 | **未知** | 历史是否提供、事件稳定键未知；**待核实** | 无上游历史合同 → 可得历史与本地同步历史区分；详情历史区 | 不将本地AuditEvent当云图检测历史；G1/G2 |
| E01 证据文本/请求响应 | **未知** | 是否可读/需脱敏/权限未知；**待核实** | 现有Evidence是旧Run引用，非云图风险证据全文 → 必要受控证据及来源身份 | 断连可读、敏感字段控制、安全文本展示；G1/G4 |
| E02 截图/二进制附件 | **未知** | 附件ID/类型/大小/权限/导出未知；**待核实** | 无同步链 → 经批准的本地受控附件及校验身份；详情附件 | 离线/过期外链、大小/类型/重定向/SSRF/撤权；G1/G4 |
| E03 附件链接、有效期与取得方式 | **未知** | 短时URL是否存在及刷新权限未知；**待核实** | 无 → 本地材料引用，外链只作可选诊断 | 不猜签名URL/抓取域名；过期外链不能算完成；G4 |
| H01 资产/服务/风险关联及历史稳定性 | **未知**（旧Run/Observation关联仅本地历史） | **待核实** | 旧历史保留 → 新来源关系/版本独立固定 | 多版本关系更正不改旧引用；G2 |

## 更新、权限和完整性合同覆盖

| ID / 语义 | 已知 / 未知 | 结果 | 保存/页面与验收要求 |
|---|---|---|---|
| S01 全量/增量与删除 | 旧页码遍历已存在；增量游标/webhook/删除通知/一致性快照**未知** | **待核实** | 先确认实际能力，无增量时有界全量；未返回/消失不自动删除或关闭。G2 |
| S02 分页期间变化/排序/ID重用 | 单次#240全量不证明长期合同；稳定排序与快照隔离**未知** | **待核实** | 分域发布、去重/补读策略必须依据合同；不能用多域时间伪装同快照。G2 |
| S03 鉴权/范围/撤销 | 旧ListIPAssets指定空间鉴权历史已验；新增操作和当前权限**未知** | **待核实**（尚不能断言权限缺失） | 精确方法/空间/凭据边界，页面/API/导出/cumora同权；G3 |
| S04 失败/空集/部分数据 | 合成fixture含鉴权/权限/上游错误；旧快照reader失败封闭 | **待核实**：新增域真实错误合同未知 | 保留最近完整版本，局部/失败另示；实际不可安全制造分支合成单列；G2/G3 |
| S05 规模/频率/限制 | 仅旧历史聚合13,182条；当前资产/风险/附件规模与许可速率**未知** | **待核实** | 不外推秒级更新/无限空间；据限制定并发、周期、附件上限。G3/G4 |
| S06 数据保留/来源撤销后处置 | 原材料私有、无客户出域已有约束；新增风险/附件保留期限与删除约定**未知** | **待核实** | 区分停同步、撤访问和受控保留；不无限保存/不绕撤权。G4 |

## 最小材料缺口

只补以下四包；优先从既有资料持有者/历史受控存放处导出**脱敏合同**，不是要求用户重复制作已有IP列表资料。不要将原始客户响应交给本会话或公开GitHub。

- **G1—实际版本的完整阅读合同包**：目标版本标识、约定资产类型/状态清单、资产列表/详情、风险列表/详情/关系、依据/历史/附件的正式API或导出文档；至少一种代表资产页和风险详情页对应脱敏样例。当前只有旧IP协议/fixture/聚合回执，不能核实完整范围。阻塞AC01A-1/2/4及相关01B实现。
- **G2—同步语义补充**：若G1已有则直接复用，不另索取；否则只补稳定ID及重用、空间/关系键、分页/排序/总数、上游变化、时间/状态/删除和可用增量语义的正式说明或获准验证证据。阻塞AC01A-3和跨批次幂等/可靠同步设计。
- **G3—本轮精确只读范围及运行限制**：环境/空间、允许方法（不只IP）、已有凭据安全使用方式、时间窗/调用上限/速率、约数规模及更新目标。旧授权不自动沿用；没有授权本轮真实读取NOT_RUN，不能据此说供应商权限必然不足。阻塞真实AC01A-5/6，不阻塞文档可达工作。
- **G4—依据/附件和数据安全约定**：实际可得材料、受控取得方式/目标网络、类型/大小/有效期、敏感字段、保留期限与撤访问/撤连接后的可见性和存储处置。G1已含部分不重复索取。阻塞相关依据/附件核验与落地，不允许以删需求解决。

Laya仓库/版本/许可、cumora部署网络分别到03/04补，不作为01A材料门槛。收到资料后的真实“接口缺失/权限缺失/明确不适用”须逐项记录依据；必需缺项未解决，不宣布完整同步，也不自行改变约定范围。
